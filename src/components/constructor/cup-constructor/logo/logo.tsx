import { FC, ReactNode, useState } from 'react';
import styles from './logo.module.css';

const Logo: FC = () => {
  return (
    <div className={styles['page']}>
       Logo
    </div>
  );
};

export default Logo;